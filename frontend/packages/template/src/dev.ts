// Dev entry point — loaded by index.html when running `pnpm dev`
// Add code here to manually exercise your package during development.

document.getElementById('app')!.textContent = 'template package — dev mode'
